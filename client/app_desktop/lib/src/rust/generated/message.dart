// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'message/friend_business_content.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

/// ===============================
/// ✅ 通用业务确认/通知（处理结果回执）
/// ===============================
/// 用于服务端向客户端回传“该业务已处理”的标准结构。
class AckContent {
  /// 是否成功
  final bool ok;

  /// 业务错误码（0 表示无错误）
  final int code;

  /// 文本信息（可本地化）
  final String message;

  /// 可选的引用消息 ID（与此次处理相关的消息）
  final BigInt? refMessageId;

  /// 附加数据（预留）
  final Uint8List extra;

  const AckContent({
    required this.ok,
    required this.code,
    required this.message,
    this.refMessageId,
    required this.extra,
  });

  @override
  int get hashCode =>
      ok.hashCode ^
      code.hashCode ^
      message.hashCode ^
      refMessageId.hashCode ^
      extra.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AckContent &&
          runtimeType == other.runtimeType &&
          ok == other.ok &&
          code == other.code &&
          message == other.message &&
          refMessageId == other.refMessageId &&
          extra == other.extra;
}

/// ======================================
/// 📨 顶层消息结构
/// ======================================
/// 定义了消息的基本框架，包含发送者、接收者、时间等元数据
class Content {
  /// 唯一消息 ID（客户端生成或服务端补全）：消息的唯一标识
  final BigInt? messageId;

  /// 消息发送方：发送消息的用户ID
  final PlatformInt64 senderId;

  /// 消息接收方（单聊为对方 ID，群聊为群 ID）：接收消息的目标
  final PlatformInt64 receiverId;

  /// 消息发送时间（毫秒时间戳）：消息创建的时间
  final PlatformInt64 timestamp;

  /// 消息所属会话类型（单聊/群聊）：消息的会话场景
  final int scene;

  /// 多段复合内容（如文本 + 图片）：消息的具体内容
  final List<MessageContent> contents;

  /// 好友申请/受理业务载荷（顶层直接表征）
  final FriendBusinessContent? friendBusiness;

  /// 群申请/受理业务载荷
  final GroupBusinessContent? groupBusiness;

  /// 标记：表示该帧仅用于连接保活（心跳）
  final bool? heartbeat;

  /// 通用业务确认/通知（处理结果回执）
  final AckContent? ack;

  /// 系统业务通知：可承载系统事件/任务执行结果
  final SystemBusinessContent? systemBusiness;

  const Content({
    this.messageId,
    required this.senderId,
    required this.receiverId,
    required this.timestamp,
    required this.scene,
    required this.contents,
    this.friendBusiness,
    this.groupBusiness,
    this.heartbeat,
    this.ack,
    this.systemBusiness,
  });

  @override
  int get hashCode =>
      messageId.hashCode ^
      senderId.hashCode ^
      receiverId.hashCode ^
      timestamp.hashCode ^
      scene.hashCode ^
      contents.hashCode ^
      friendBusiness.hashCode ^
      groupBusiness.hashCode ^
      heartbeat.hashCode ^
      ack.hashCode ^
      systemBusiness.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Content &&
          runtimeType == other.runtimeType &&
          messageId == other.messageId &&
          senderId == other.senderId &&
          receiverId == other.receiverId &&
          timestamp == other.timestamp &&
          scene == other.scene &&
          contents == other.contents &&
          friendBusiness == other.friendBusiness &&
          groupBusiness == other.groupBusiness &&
          heartbeat == other.heartbeat &&
          ack == other.ack &&
          systemBusiness == other.systemBusiness;
}

/// ===============================
/// 🤝 好友业务消息
/// ===============================
/// 将申请与审核组织在同一个业务载体内，便于 socket 推送和前端统一渲染
class FriendBusinessContent {
  final Action? action;

  const FriendBusinessContent({this.action});

  @override
  int get hashCode => action.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FriendBusinessContent &&
          runtimeType == other.runtimeType &&
          action == other.action;
}

/// 好友申请处理（接受/拒绝），加入默认消息字段以便自动下发欢迎/提示
class FriendRequestDecisionPayload {
  final BigInt requestId;
  final bool accepted;
  final String remark;
  final PlatformInt64 decidedAt;
  final bool sendDefaultMessage;
  final String defaultMessage;

  /// 审批人希望展示给申请人的别名/称呼
  final String alias;

  const FriendRequestDecisionPayload({
    required this.requestId,
    required this.accepted,
    required this.remark,
    required this.decidedAt,
    required this.sendDefaultMessage,
    required this.defaultMessage,
    required this.alias,
  });

  @override
  int get hashCode =>
      requestId.hashCode ^
      accepted.hashCode ^
      remark.hashCode ^
      decidedAt.hashCode ^
      sendDefaultMessage.hashCode ^
      defaultMessage.hashCode ^
      alias.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FriendRequestDecisionPayload &&
          runtimeType == other.runtimeType &&
          requestId == other.requestId &&
          accepted == other.accepted &&
          remark == other.remark &&
          decidedAt == other.decidedAt &&
          sendDefaultMessage == other.sendDefaultMessage &&
          defaultMessage == other.defaultMessage &&
          alias == other.alias;
}

/// 好友申请信息
class FriendRequestPayload {
  final BigInt requestId;
  final PlatformInt64 fromUserId;
  final PlatformInt64 toUserId;
  final String reason;
  final int source;
  final PlatformInt64 createdAt;
  final String remark;

  /// 申请人想展示的别名/昵称（可用于邀请卡片）
  final String alias;

  const FriendRequestPayload({
    required this.requestId,
    required this.fromUserId,
    required this.toUserId,
    required this.reason,
    required this.source,
    required this.createdAt,
    required this.remark,
    required this.alias,
  });

  @override
  int get hashCode =>
      requestId.hashCode ^
      fromUserId.hashCode ^
      toUserId.hashCode ^
      reason.hashCode ^
      source.hashCode ^
      createdAt.hashCode ^
      remark.hashCode ^
      alias.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FriendRequestPayload &&
          runtimeType == other.runtimeType &&
          requestId == other.requestId &&
          fromUserId == other.fromUserId &&
          toUserId == other.toUserId &&
          reason == other.reason &&
          source == other.source &&
          createdAt == other.createdAt &&
          remark == other.remark &&
          alias == other.alias;
}

/// ===============================
/// 👥 群业务消息
/// ===============================
/// 将加群申请与审核封装在同一业务载体
class GroupBusinessContent {
  final Action? action;

  const GroupBusinessContent({this.action});

  @override
  int get hashCode => action.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GroupBusinessContent &&
          runtimeType == other.runtimeType &&
          action == other.action;
}

/// ======================================
/// 💠 消息内容结构（oneof 类型）
/// ======================================
/// 使用 oneof 定义不同类型的消息内容，确保消息类型的互斥性
class MessageContent {
  final Content? content;

  const MessageContent({this.content});

  @override
  int get hashCode => content.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MessageContent &&
          runtimeType == other.runtimeType &&
          content == other.content;
}

class SystemBusinessContent {
  /// 类型：允许预定义枚举以便客户端/服务端统一处理
  final int businessType;

  /// 标题
  final String title;

  /// 详细内容（可用 JSON 等结构）
  final String detail;

  /// 附加上下文，如关联 ID、发起方等
  final Map<String, String> metadata;

  /// 短摘要（可用于列表）
  final String? summary;

  /// 具体正文内容或跳转文案
  final String? body;

  /// 展示区域（例如首页/弹窗/ banner）
  final int displayArea;

  /// 关联跳转地址
  final String? actionUrl;
  final PlatformInt64? validFrom;
  final PlatformInt64? validTo;

  const SystemBusinessContent({
    required this.businessType,
    required this.title,
    required this.detail,
    required this.metadata,
    this.summary,
    this.body,
    required this.displayArea,
    this.actionUrl,
    this.validFrom,
    this.validTo,
  });

  @override
  int get hashCode =>
      businessType.hashCode ^
      title.hashCode ^
      detail.hashCode ^
      metadata.hashCode ^
      summary.hashCode ^
      body.hashCode ^
      displayArea.hashCode ^
      actionUrl.hashCode ^
      validFrom.hashCode ^
      validTo.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SystemBusinessContent &&
          runtimeType == other.runtimeType &&
          businessType == other.businessType &&
          title == other.title &&
          detail == other.detail &&
          metadata == other.metadata &&
          summary == other.summary &&
          body == other.body &&
          displayArea == other.displayArea &&
          actionUrl == other.actionUrl &&
          validFrom == other.validFrom &&
          validTo == other.validTo;
}
