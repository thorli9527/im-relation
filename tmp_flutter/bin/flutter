#!/bin/sh
case "$1" in
  --version|-v)
    printf 'Flutter 0.0.0\n'
    exit 0
    ;;
  pub)
    shift
    printf 'flutter pub %s\n' "$*" >&2
    exit 0
    ;;
  *)
    printf 'Flutter stub invoked with args: %s\n' "$@" >&2
    exit 0
    ;;
esac
