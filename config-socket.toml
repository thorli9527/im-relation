[server.grpc]
host = "0.0.0.0"
port = 9600
[server.http]
host = "0.0.0.0"
port = 9700
[sys]
log_leve = "info"
[app_socket]
index = 0
# Socket TCP 监听（内部地址）
host = "0.0.0.0"
port = 9900
# Socket 对外暴露地址（客户端连接用，pub_port=0 表示无需指定端口）
pub_host = "0.0.0.0"
pub_port = 9900
# ACK 分片数（不配置则为 CPU 核数）
ack_shards = 0
# ACK 重试间隔（毫秒）
ack_retry_ms = 500
# 分片调度器分片数（不配置则为 CPU 核数）
dispatch_shards = 0
# 每分片队列容量
dispatch_cap = 10000

[kafka]
broker = "127.0.0.1:9092"
group_id = "socket-dispatcher"
replicas = 1
kafka_user_name=""
kafka_password=""


#------------friend_service
[friend_service]
index = 0
grpc_addr = "127.0.0.1:8002"

#------------app_group_dispatcher
[group_service]
index = 0
grpc_addr = "127.0.0.1:8003"

#------------user_service
[user_service]
index = 0
grpc_addr = "127.0.0.1:6001"

#------------message_service
[msg_friend_nodes]
index = 0
grpc_addr = "127.0.0.1:8102"

#------------group_service
[msg_group_nodes]
index = 0
grpc_addr = "127.0.0.1:8103"
